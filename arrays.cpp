#include <iostream>

int main(void)
{
    int guesses[7];
    bool is_true;

    guesses[0] = 1;
    guesses[1] = 2;
    guesses[2] = 3;

    std::cout << sizeof(is_true) << std::endl;
    return 0;
}